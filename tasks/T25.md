# T25: HD Wallet — HdBadge and DerivedAddressList Components

**Status**: todo
**Layer**: frontend
**Assignee**: developer
**Depends on**: T24

## Context

This task creates two new Vue components: `HdBadge.vue` (a small accessible badge that displays "HD" text next to the wallet tag) and `DerivedAddressList.vue` (the expandable table of active derived addresses). These components are self-contained and reused in both `WalletTable.vue` (T26) and `WalletDetailView.vue` (T26).

> "frontend/src/components/wallet/HdBadge.vue — NEW: 'HD' badge component"
> — TECH_SPEC_HD_WALLETS.md §3

> "frontend/src/components/wallet/DerivedAddressList.vue — NEW: Expandable derived address table"
> — TECH_SPEC_HD_WALLETS.md §3

## Files owned

- `frontend/src/components/wallet/HdBadge.vue` (create)
- `frontend/src/components/wallet/DerivedAddressList.vue` (create)
- `tests/frontend/components/HdBadge.test.ts` (create)
- `tests/frontend/components/DerivedAddressList.test.ts` (create)

## Subtasks

- [ ] ST1: Create `HdBadge.vue` as a `<span>` with Tailwind classes `inline-flex items-center px-1.5 py-0.5 rounded text-xs font-bold bg-orange-100 text-orange-800 dark:bg-orange-900 dark:text-orange-200`, `aria-label="HD Wallet"`, and `title="Hierarchical Deterministic Wallet (xpub/ypub/zpub)"` per TECH_SPEC_HD_WALLETS.md §7.1
- [ ] ST2: Create `DerivedAddressList.vue` with four display states: loading (spinner), error, empty ("No transactions found for this HD wallet yet."), and populated table — per §7.1
- [ ] ST3: `DerivedAddressList.vue` props: `addresses: DerivedAddressResponse[] | null`, `totalAddressCount: number | null`, `loading: boolean`, `error: boolean`
- [ ] ST4: Implement address truncation inline in `DerivedAddressList.vue`: `formatAddress(addr)` → first 8 chars + "..." + last 6 chars (FR-H09 for derived addresses)
- [ ] ST5: Show "Showing top N of M addresses." note when `totalAddressCount > addresses.length` (FR-H15)
- [ ] ST6: Import and use `formatBtc` and `formatUsd` from `@/utils/format` for balance display
- [ ] ST7: Create `tests/frontend/components/HdBadge.test.ts` — renders "HD" text and has `aria-label` attribute
- [ ] ST8: Create `tests/frontend/components/DerivedAddressList.test.ts` covering all 6 scenarios in TECH_SPEC_HD_WALLETS.md §9.4

## Acceptance criteria

- [ ] `cd frontend && npm run test` passes with no failures
- [ ] `cd frontend && npm run build` succeeds with no TypeScript errors
- [ ] `HdBadge` renders the text "HD" and has `aria-label="HD Wallet"`
- [ ] `DerivedAddressList` with `loading=true` shows a loading spinner (not address rows)
- [ ] `DerivedAddressList` with `error=true` shows "Could not load address breakdown. Will retry on next refresh."
- [ ] `DerivedAddressList` with `addresses=[]` shows "No transactions found for this HD wallet yet."
- [ ] `DerivedAddressList` with populated `addresses` renders one row per address with truncated address, BTC balance, USD balance
- [ ] `DerivedAddressList` shows "Showing top 200 of 350 addresses." when `addresses.length=200` and `totalAddressCount=350`
- [ ] FR-H13: Only active addresses (those with n_tx > 0) appear — enforced by the backend; the component renders whatever is passed
- [ ] FR-H14: Addresses are displayed in descending balance order — enforced by the repository; the component renders in prop order
- [ ] FR-H30: "HD" badge conveys information by text, not color alone (accessible)
- [ ] TECH_SPEC_HD_WALLETS.md §7.1 fully implemented

## Notes

- Import `LoadingSpinner` from `@/components/common/LoadingSpinner.vue` (already exists in the codebase).
- Use `v-if` / `v-else-if` / `v-else` for the four states as shown in §7.1 — do not use dynamic component switching.
- The `DerivedAddressList.vue` component is stateless — it displays what it receives via props. It has no local fetch logic.
- The expand/collapse toggle is managed by the parent (`WalletTable.vue` or `WalletDetailView.vue`), not by this component.
